<div class="last-story-card">
  <div class="last-story-title">
    <h2><%= link_to last_story.title, story_path(last_story) %>
    </h2>
  </div>
  <div class="last-story-text">
  <p><%= last_story.text %></p>
  </div>
  <div class="last-story-category">
    <p><%= link_to last_story.category, filter_index_stories_path(:category) %></P>
  </div>
  <div class="last-story-like">
    <p><%= last_story.likes.count%> <%= (last_story.likes.count) == 1 ? 'Like' : 'Likes'%></p>
  </div>
</div>
