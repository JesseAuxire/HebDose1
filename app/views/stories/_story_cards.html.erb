<div class="story-card">
  <div class="story-title">
    <h2><%= link_to story.title, story_path(story) %>
    </h2>
  </div>
  <div class="story-text">
  <p><%= story.text %></p>
  </div>
  <div class="story-category">
    <p><%= link_to story.category, filter_index_stories_path(@category) %></P>
  </div>
</div>
